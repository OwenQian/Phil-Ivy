#ifndef PLAYER_H
#define PLAYER_H

#include <vector>

class Player() {
	public:

	protected:
		std::vector<int> cards;
		int chips;
		std::vector<int> myHistory;
		std::vector<std::vector<int> > oppHistory;
}
#endif
